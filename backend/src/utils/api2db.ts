type NameFieldApi = {
  language: {
    name: string
    url?: string
  }
  name: string
}

type NamesFieldDb = {
  name: string
}

// TODO: implement filtering the names array from pokeapi to db form
export const generateNamesField = (namesApi: NameFieldApi[]) => {
  namesApi
  const test: NamesFieldDb | null = null
  test
}
